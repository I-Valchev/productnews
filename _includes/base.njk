<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="No more spammy emails and notifications.
        Only unobtrusive news aggregation that you can review at your own pace.
    ">

    <title>Product News</title>
    {% include 'og-tags.njk' %}

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="icon" type="image/svg" href="{{ '/assets/favicon.svg'|url }}">
    <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,wght@0,400;0,700;1,400;1,800&display=swap" rel="stylesheet">
    <link href="{{ '/assets/splendor.min.css'|url }}" rel="stylesheet" />
    <link href="{{ '/assets/styles.css'|url }}" rel="stylesheet" />
    <script src="{{ '/assets/luxon.min.js'|url }}" defer="defer"></script>
    <script src="{{ '/assets/scripts.js'|url }}" defer="defer"></script>
</head>
<body>

<nav>
    <a href="{{ '/'|url }}">Home</a> |
    <a href="{{ '/contact'|url }}">Add feed</a>
</nav>

{% if page.url == '/' %}
    <div style="background-color: var(--primary);">
        <header>
            <h1>Product news <b><i>you need</i></b></h1>
            <h2 style="font-size: 1.3rem; margin-top: -1rem;">No more spammy emails and notifications. Only unobtrusive news aggregation
                that you can review at your own pace.</h2>
        </header>
    </div>
{% endif %}

{% block content %}
{% endblock %}

</body>
</html>
